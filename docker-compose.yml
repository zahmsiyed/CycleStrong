services:
  db:
    image: postgres:16
    container_name: cyclestrong-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: cyclestrong
      POSTGRES_PASSWORD: cyclestrong
      POSTGRES_DB: cyclestrong
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
